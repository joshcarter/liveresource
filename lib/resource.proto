package resource;

message Null {
}

message ResponseHeader {
  enum MessageStatus {
    QUEUED = 0;
    IN_PROGRESS = 1;
    DONE = 2;
  }

  required MessageStatus status = 1;
  optional string error = 2;
}

enum Status {
  OK = 0;
  FAILED = 1;
}

message FanStatus {
  ResponseHeader header;

  required Status status = 1;
  optional uint32 tach = 2;
}

service Fan {
  rpc Status (Null) returns (FanStatus);
}
